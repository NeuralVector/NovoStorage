pre-commit:
  commands:
    format:
      run: pnpm exec oxfmt --write --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true

pre-push:
  commands:
    typecheck:
      run: pnpm exec tsc --noEmit